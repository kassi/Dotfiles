#!/bin/bash

file="$1"
if [ -z "$file" ]; then
  echo "Usage: brew-bundle <Brewfile>"
  exit 1
fi

while read -r line || [[ -n $line ]]
do
  command=${line/##*/}
  if [ -n "$command" ]; then
    brew $command
  fi
done < "$file"
